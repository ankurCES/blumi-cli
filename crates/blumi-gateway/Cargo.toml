[package]
name = "blumi-gateway"
description = "messaging gateways + voice (feature-gated)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
