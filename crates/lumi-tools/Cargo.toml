[package]
name = "lumi-tools"
description = "built-in tools + JSON-Schema validation + layered execution pipeline."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
